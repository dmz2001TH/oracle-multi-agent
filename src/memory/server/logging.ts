/**
 * Server Logging — stub for oracle tools
 */

export function logSearch(
  _query: string,
  _type: string,
  _mode: string,
  _resultsCount: number,
  _searchTimeMs: number,
  _results: any[] = [],
  _project?: string
): void {
  // Stub — logging disabled in minimal mode
}
